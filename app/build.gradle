apply plugin: 'com.android.application'

android {
    compileSdkVersion 21
    buildToolsVersion "21.1.1"

    defaultConfig {
        applicationId "com.devatma.wear.notifications4watch"
        minSdkVersion 21
        targetSdkVersion 19
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
    }

}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
